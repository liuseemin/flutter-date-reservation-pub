name: flutter_date_reserve
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.0
  cloud_firestore: ^5.2.0
  calendar_date_picker2: ^0.5.3
  csv: ^6.0.0
  universal_html: ^2.2.0
  flutter_localizations:
    sdk: flutter
  web: ^1.1.1
  js: ^0.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
